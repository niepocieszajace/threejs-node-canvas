# threejs-node-canvas
Simple POC that checks if it's possible to render Three.JS sceen witih node.js without browser

# Run project
  1. Install required dependencies eg. `pnpm install`
  2. Run `index.js` file eg. `node index.js`
  3. Check `dist` folder for result 
